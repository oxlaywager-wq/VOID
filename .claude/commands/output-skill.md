# OUTPUT SKILL — Code 100% Complet

**RÈGLE ABSOLUE** : Zéro placeholder, zéro raccourci, zéro commentaire du type "// reste du code ici".

## Enforcement

Quand tu génères du code :
1. Chaque fichier doit être **entièrement complet** — du premier import à l'export final
2. **Aucun** `// ... rest of component` ou `// similar for other items`
3. **Aucun** `/* TODO */` ou `// implement later`
4. Si un composant est long → tu le codes entièrement quand même
5. Les données de test/mock sont **réelles** (vrais noms, vrais textes, vraies URLs)

## Format de sortie

Toujours utiliser des blocs de code avec le chemin du fichier :
```tsx
// src/components/ui/MonComposant.tsx
[CODE COMPLET]
```

## Vérification avant envoi
- [ ] Tous les imports sont présents
- [ ] Toutes les fonctions sont implémentées
- [ ] Tous les composants sont exportés
- [ ] Aucun placeholder textuel
- [ ] Le code peut être copié-collé et fonctionne immédiatement
